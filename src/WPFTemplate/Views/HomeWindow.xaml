<Window x:Class="WPFTemplate.Views.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="HomeWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="{Binding Path=Count}"></TextBlock>
        <Button Width="50" Height="20"
                Command="{Binding AddCount}"
                CommandParameter="{Binding}">增加</Button>
        <Button Width="50" Height="20"
                Command="{Binding MiniCount}"
                CommandParameter="{Binding}">减少</Button>
    </StackPanel>
</Window>